# 보안 정책과 한계

Slide Atlas는 공개 데모이며, 조직에서 사용하는 서비스 수준의 보안 요건을 모두 충족하지는 않습니다. 공개 데모에 개인정보, 고객 자료, 조직의 비공개 템플릿이나 기밀 내용을 입력하지 마세요.

## 적용한 보호 조치

- 무작위 256비트 세션 토큰을 `HttpOnly`, `SameSite=Lax` 쿠키로 발급하며, HTTPS에서는 `Secure` 속성도 적용합니다. 서버에는 토큰의 SHA-256 해시만 저장합니다.
- 작업 데이터 조회와 변경은 서버가 세션에서 확인한 작업 공간으로 제한합니다. 읽기 전용 공유는 별도의 무작위 링크로 발급한 사본에만 접근합니다. 요청자가 URL이나 JSON에 작업 공간 ID를 넣어 다른 공간을 선택할 수 없습니다.
- SQL 파라미터 바인딩, Zod 입력 검증, JSON 형식 제한, 스트리밍 요청 본문의 크기 제한, 요청 출처 확인, 검수 상태 전이 규칙을 적용합니다.
- 데이터 변경과 감사 이력을 하나의 트랜잭션으로 기록하며, 동시 수정 충돌을 감지하면 기존 내용을 덮어쓰지 않고 오류를 반환합니다.
- API 응답에 `private, no-store`를 설정하고 SVG·XML에 들어가는 텍스트를 이스케이프 처리합니다. 내보내기는 첨부 파일 응답으로 제공하며, 사용자 HTML을 실행하거나 외부 이미지를 가져오지 않습니다.
- CSP, 프레임 삽입 차단, `nosniff`, Referrer Policy를 적용합니다. `unsafe-eval`은 개발 환경에서만 허용합니다. 운영 환경에는 인라인 스크립트와 스타일을 허용하므로 nonce 기반의 엄격한 CSP는 아닙니다.
- 새로 복제한 프로젝트에서는 실제 AI 호출을 기본적으로 비활성화합니다. 공개 데모는 초대 코드가 있어야 AI를 호출할 수 있으며, 서버 전용 키, 출력 검증, 25초 응답 제한과 서비스 전체의 하루 30회 요청 한도를 적용합니다. 여러 서버 인스턴스가 Neon PostgreSQL을 통해 요청 수를 공유합니다.
- OpenAI 키와 초대 코드는 Vercel의 Production 환경에만 `Secret`으로 저장했습니다. 로컬에서는 Git에서 제외한 `.env.local`을 사용하며, `NEXT_PUBLIC_` 변수나 배포 소스 파일에 넣지 않습니다. [비밀값 노출 검사 기록](docs/secret-exposure-verification.json)에서 확인 범위와 한계를 확인하실 수 있습니다.
- 클라이언트 코드나 오류 응답에 API 자격 증명을 포함하지 않습니다. 모델이 반환한 본문과 사용자가 입력한 브리프는 로그에 남기지 않습니다.

## 현재의 한계

- 익명 쿠키는 방문자별 데모 데이터를 구분합니다. 자체 계정에 연결하면 다시 로그인할 수 있으며 소유자, 작성자, 검수자와 열람자의 권한을 서버에서 검사합니다. SSO와 비밀번호 분실 복구는 제공하지 않습니다. 익명 쿠키를 지우면 연결하지 않은 작업에 다시 접근하지 못할 수 있습니다.
- PostgreSQL의 행 수준 보안(RLS)은 적용하지 않았습니다. 현재는 데이터 접근 계층의 쿼리로 작업 공간을 구분하고, 서로 다른 세션의 접근을 테스트합니다. 검수자 권한은 API 계층에서 분리하며 조직용 서비스로 확장할 때는 DB 차원의 보호 조치와 조직별 정책 검토가 추가로 필요합니다.
- AI 전체 한도를 제외한 요청 제한은 익명 세션별로 적용합니다. 새로운 세션을 만들면 제한을 우회할 수 있으므로 봇 방어 수단으로 볼 수는 없습니다. 규모를 확대하기 전 플랫폼 방화벽과 요청 제한 정책을 함께 적용해야 합니다.
- AI 초대 코드는 실험 참여자를 위한 공유 비밀값이며 사용자별 계정이나 권한을 대신하지 않습니다. 공개 문서에 게시하지 마시고, 노출된 경우 서버 설정에서 교체해 주세요. 일별 한도는 요청 횟수 제한으로, 제공업체의 청구 금액 상한을 설정한 것은 아닙니다.
- 작업 공간과 저장 항목 수에 상한을 두었지만, 여러 서버가 동시에 접근하는 상황까지 엄밀하게 통제하는 분산 할당량은 구현하지 않았습니다. 부하나 서비스 거부 공격에 대한 별도 인증도 받지 않았습니다.
- 익명 공간은 접근 시 7일 만료 여부를 확인하고, 만료된 데이터는 다음 작업 공간 생성 시 삭제합니다. 계정에 연결된 공간은 익명 데이터 정리에서 제외합니다. 일정한 주기로 백그라운드 삭제를 수행하거나 삭제 완료 시간을 보장하지는 않습니다.
- 서버에서는 글꼴 폭을 추정하고, 브라우저에서는 현재 SVG의 텍스트 경계도 측정합니다. PowerPoint 렌더링을 동일하게 보장하지는 않습니다. 수치 일치 검사는 부호와 명시된 단위의 변형을 확인하지만 단위 환산, 사실 여부와 수치가 어떤 주장에 연결되는지까지 판정하지 못하므로 사람이 결과를 검토해야 합니다.
- 템플릿 과거 버전은 변경 불가능한 JSONB 사본으로 보관하고 프레젠테이션은 작업 공간, 템플릿 ID와 버전으로 해당 사본을 참조합니다. 저장소 계층에서 존재 여부와 승인 상태를 검사하며 DB 외래 키를 사용하는 방식은 아닙니다. 선택 일괄 갱신은 소유자와 작성자만 실행할 수 있고 각 프레젠테이션의 버전과 내용 매핑을 검사합니다. 과거 버전 복원은 제공하지 않습니다.
- JSON 가져오기는 크기와 구조가 제한된 온톨로지 데이터만 허용합니다. PPTX 가져오기는 8MB, ZIP 항목 400개, 압축 해제 예상 크기 32MB, 앞쪽 12장으로 제한하고 암호화 파일을 거절합니다. OOXML의 텍스트와 좌표만 읽으며 매크로·외부 링크·미디어를 실행하거나 불러오지 않습니다.
- PPTX 추출은 이미지, 표, 차트와 마스터를 복원하지 않고 글꼴 크기와 배치를 휴리스틱으로 해석합니다. 신뢰도는 후보 검토 순서를 돕는 규칙 기반 값이며 정확도를 보증하지 않으므로, 저장 전에 운영자가 초안을 확인해야 합니다.

## 문제를 발견하셨을 때

저장소에서 비공개 취약점 제보 기능이 활성화되어 있다면 해당 기능을 이용해 주세요. 활성화되어 있지 않다면 자격 증명, 사용자 내용, 다른 방문자의 데이터를 노출하는 재현 자료를 제외하고 영향 범위를 이슈로 알려 주세요. API 키를 이슈나 채팅에 붙여넣지 마시고, 이미 노출된 키는 제공업체에서 폐기하고 새 키로 교체해 주세요.

## 자체 계정과 공유

- 비밀번호는 무작위 salt를 사용하는 scrypt로 처리합니다. 계정 세션, 팀 초대와 공유 링크 토큰은 서버에 SHA-256 해시로 저장합니다. 로그인 응답은 HttpOnly, SameSite=Lax 쿠키를 사용하고 HTTPS에서 Secure를 적용합니다.
- 계정 이름별 로그인 시도와 계정별 팀 변경 요청을 제한합니다. 전역 봇 방어, 다중 요소 인증, 이메일 인증과 비밀번호 분실 복구는 제공하지 않습니다.
- 계정에 연결된 작업 공간에는 이전 익명 쿠키로 접근할 수 없습니다. 구성원 권한 해제 시 해당 공간의 계정 세션을 삭제하고, 비밀번호 변경 시 계정의 다른 세션도 종료합니다.
- 공유 링크를 발급하면 저장본의 읽기 전용 사본을 만듭니다. 원문 브리프, 근거 연결과 생성 사용량은 제외하지만 화면에 표시되는 내용과 사용한 템플릿 사본은 포함합니다. 링크 소지자는 유효 기간 안에 열람할 수 있습니다. 공유 해제 후에도 이미 복사한 내용까지 회수할 수는 없습니다.
- 브라우저 초안은 IndexedDB에 보관하므로 공용 기기에서는 초안 삭제와 서버 저장 여부를 확인해야 합니다. 계정 로그아웃이 브라우저의 모든 로컬 데이터를 지우는 동작은 아닙니다. 초안은 현재 작업 공간에 해당할 때만 복구 화면에 표시합니다.

## 작업과 평가 데이터

작업 입력·결과, 품질 평가 사본과 사람 판정은 작업 공간으로 격리하고 공개 공유에 포함하지 않습니다. 원문이 포함된 회귀 입력의 다운로드와 비공개 라이브 평가 파일은 사용자가 직접 관리합니다. 모델 초대 코드는 작업이나 임베딩 캐시에 보관하지 않습니다. 의미 검색은 입력 검색어와 승인 템플릿의 설명·슬롯 정보를 외부 모델에 전송하며, 새 요청에 기존 AI 예산과 초대 코드 검사를 적용합니다.

감사 계정은 요청의 서버 세션에서 가져오며 입력 JSON의 계정 정보를 신뢰하지 않습니다. 검수 요청과 승인 권한을 분리하고, 품질 평가는 사본 등록자 외 두 계정의 판정을 요구합니다. 서로 다른 계정이 실제로 독립된 사람인지 자동으로 입증하지는 않습니다. 작업 취소는 외부 모델의 과금이나 이미 완료된 항목을 되돌리지 않습니다.

작업 입력, 평가 사본과 변경 영향 적용에는 1MiB 요청 제한을 적용합니다. 일반 JSON 요청의 64,000바이트 제한은 유지합니다. 비공개 평가 파일은 `*.private.json` 규칙으로 Git에서 제외합니다.
