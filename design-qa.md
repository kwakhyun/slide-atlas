# 디자인 QA — 2026-09-05

요청 범위는 기존 제품의 전체 UI/UX를 밝은 디자인 도구의 분위기로 재구성하는 것입니다. 외부 서비스의 화면과 로고를 그대로 복제하는 검사가 아니며, 프로젝트의 편집, 검수와 데이터 흐름을 기준으로 확인했습니다.

## 검토 근거

Product Design의 화면 감사 절차에 따라 현재 로컬 빌드에서 기본 화면과 실제 동작을 확인했습니다. 이전 리메이크에서 선택한 흰 배경, 청록색 주요 동작, 중립색 글자와 둥근 카드를 기준으로 마감 상태를 점검했습니다.

- 현재 UI 근거: [스튜디오](docs/screenshots/studio.png), [템플릿](docs/screenshots/library.png), [검수](docs/screenshots/review.png), [실험실](docs/screenshots/experiments.png), [계정](docs/screenshots/team.png), [가이드](docs/screenshots/about.png), [모바일](docs/screenshots/mobile.png).
- 문서 이미지는 1440 × 1000px와 390 × 844px의 보이는 영역을 촬영합니다. [촬영 상태와 재현 방법](docs/screenshots/README.md)에 상세 조건을 기록했습니다.
- 자동 검증은 6개 화면의 데스크톱/모바일, 템플릿 등록 대화상자, 768px/1024px 편집을 포함합니다. 이전 리메이크에서는 펼친 댓글, 브랜드와 재생성 패널도 별도로 검사했습니다.
- 이전에 사용한 외부 참고 화면과 중간 캡처는 로컬 검토 자료이며 저장소에 포함하지 않습니다. 위 링크는 모두 저장소에서 열 수 있는 최신 제품 화면입니다.

## 확인 결과

| 항목            | 확인                                                                                                                                                                   |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 글꼴과 계층     | 기존 Pretendard를 유지하고 본문은 12–14px, 화면 제목은 22–27px로 구분했습니다. 설명과 제목의 간격, 작은 글자 대비와 모바일 줄바꿈을 확인했습니다.                      |
| 배치와 간격     | 데스크톱 탐색 메뉴는 일반 화면 216px, 편집기 88px입니다. 캔버스와 입력 패널을 연결하고 주요 동작은 상단에 배치했습니다. 추가 패널은 펼치면 전체 폭을 사용합니다.       |
| 색상            | `--accent: #007a83`, 흰색 패널, `#f7f8fa` 배경을 사용합니다. 경고와 오류 색상은 별도로 유지했습니다. 작은 글자에 4.5:1 미만의 조합이 남지 않도록 axe로 확인했습니다.   |
| 이미지와 아이콘 | 실제 템플릿의 SVG 미리보기를 사용합니다. UI 로고와 탭 아이콘은 Lucide의 PanelsTopLeft이며 임의로 그린 외부 로고가 아닙니다. 외부 서비스 자산은 제품에 넣지 않았습니다. |
| 문구            | 작업 이름과 동작을 한국어로 표시하고 제목을 간결하게 정리했습니다. 제품 소스와 정적 자산에서 참고 서비스명은 검색되지 않았습니다.                                      |
| 동작            | 최종 전체 E2E 45개와 단위·통합 95개가 통과했습니다. 주요 변경은 시각 체계와 레이아웃이며 서버 API 계약을 변경하지 않았습니다.                                          |

## 수정 이력

1. **[P2, 수정 완료] 작은 글자 대비**: 캔버스 비율, 탐색 안내, 선택 카테고리의 수량, 검수와 실험 설명의 색상 대비가 부족했습니다. 전경색을 진하게 바꾸고 6개 화면의 데스크톱/모바일 axe 검사를 다시 통과했습니다.
2. **[P1, 수정 완료] 모바일 작업 영역 폭**: 공통 사이드바 토큰이 모바일 값을 덮어써 편집 내용이 화면 밖으로 나갔습니다. 모바일 토큰을 0으로 재정의하고 하단 탐색 메뉴 배치를 명시했습니다. 페이지 넘침을 숨기는 방식은 사용하지 않았습니다.
3. **[P2, 수정 완료] 관리 패널 정렬과 미리보기 라벨**: 부분 재생성 패널에도 동일한 그리드 규칙을 적용했습니다. 카드의 레이아웃 라벨과 슬라이드가 겹치지 않도록 위쪽 여백을 분리했습니다.
4. **태블릿 검사 수정**: 800px 이하에서는 먼저 내용 입력으로 전환해야 합니다. 추가 테스트가 실제 사용 순서에 맞춰 입력, 미리보기와 되돌리기를 수행하도록 수정했습니다. 768px/1024px 두 시나리오가 통과했습니다.

5. **[P2, 수정 완료] 계정 입력 흐름**: 지나치게 넓은 입력 영역을 줄이고 계정 만들기를 주요 버튼으로 구분했습니다. 이름과 비밀번호 조건을 각 입력란에 연결하고 기본 입력 검증, 키보드 제출, 로딩 안내를 추가했습니다. 세션 복구 동작은 별도 펼침 영역으로 옮겼습니다.
6. **[P2, 수정 완료] 화면과 문서의 불일치**: 가이드의 글자 넘침 검사와 계정 지원 설명을 현재 기능에 맞췄습니다. 데모 스크립트의 이전 화면 제목을 수정하고 스크린샷 7장과 영상을 다시 촬영했습니다. 과거 AI 검증과 감사 이미지는 당시 기록임을 명시했습니다.

## 사용자 흐름 최종 확인

| 단계             | 상태 | 확인 내용                                                |
| ---------------- | ---- | -------------------------------------------------------- |
| 1. 진입과 탐색   | 통과 | 제목, 현재 위치, 모바일 하단 탐색, 가이드의 기능 설명    |
| 2. 생성과 편집   | 통과 | 내용 입력, 저장, 되돌리기, 출력과 태블릿 도구 위치       |
| 3. 템플릿과 검수 | 통과 | 검색과 필터, 등록 대화상자, 검수 흐름과 이력             |
| 4. 실험과 협업   | 통과 | 실험 실행, 계정 만들기와 키보드 로그인, 공유 생성과 해제 |
| 5. 문서에서 확인 | 통과 | 실제 화면 캡처, 녹화 스크립트, 현재와 과거 검증 구분     |

## 검증 범위

6개 기본 화면과 템플릿 등록 대화상자, 펼친 관리 패널을 검사했습니다. 브라우저 오류는 발견되지 않았고, 기능 흐름과 페이지 가로 넘침 검사를 통과했습니다. PPTX 출력 기능은 기존 E2E에 포함되며 PowerPoint 애플리케이션의 실제 표시와 외부 계정 운영 검증은 이번 디자인 검사에 포함하지 않았습니다.

- [x] 공통 색상과 반응형 탐색 체계 적용
- [x] 편집, 탐색, 검수, 실험, 계정과 공유 흐름 확인
- [x] 대비와 가로 넘침 회귀 수정
- [x] 타입 검사, 린트와 프로덕션 빌드 통과
- [x] 마지막 모바일 카드 캡처 재확인

final result: passed

이번 검토에서 발견한 항목은 모두 수정했습니다. 검증 범위 안에서 미해결 P0/P1/P2 항목은 없으며, 자동 검사 통과가 모든 기기와 보조기술에 대한 접근성을 보장하지는 않습니다.
